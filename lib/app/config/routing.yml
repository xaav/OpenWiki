wiki_edit:
    pattern: /{page}/edit
    defaults: { _controller: XaavWikiBundle:Default:edit }

wiki_view:
    pattern:   /{page}
    defaults:  { _controller: XaavWikiBundle:Default:view }
    
wiki_index:
    pattern: /
    defaults: { _controller: XaavWikiBundle:Default:view }
