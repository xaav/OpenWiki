parameters:
    git_repository.class: Glip\Git
    git_repository.data_directory: "./pages/.git"

services:
    git_repository:
        class: %git_repository.class%
        arguments: [%git_repository.data_directory%]